COMPOSE_PROJECT_NAME=owuicore

# --- Images ---
OPENWEBUI_IMAGE=ghcr.io/open-webui/open-webui:v0.8.12
OPENWEBUI_ENV=prod
PIPELINES_IMAGE=ghcr.io/open-webui/pipelines:main
SEARXNG_IMAGE=docker.io/searxng/searxng:latest
VALKEY_IMAGE=docker.io/valkey/valkey:8-alpine

# --- Ports ---
OPENWEBUI_PORT=3000
PIPELINES_PORT=9099
KEYCLOAK_PORT=8082
SEARXNG_LOCAL_PORT=8083

# --- URLs ---
OPENWEBUI_PUBLIC_URL=http://localhost:3000
KEYCLOAK_PUBLIC_URL=http://localhost:8082
KEYCLOAK_INTERNAL_URL=http://keycloak:8080
KEYCLOAK_REALM=openwebui
SEARXNG_LOCAL_BASE_URL=http://localhost:8083

# --- Scaleway LLM ---
SCW_LLM_BASE_URL=https://api.scaleway.ai/<PROJECT_ID>/v1
SCW_SECRET_KEY_LLM=CHANGE_ME
SCW_LLM_MODEL=mistral-small-3.2-24b-instruct-2506
OPENAI_EMBEDDING_MODEL=bge-multilingual-gemma2

# --- Secrets ---
PIPELINES_API_KEY=CHANGE_ME
WEBUI_SECRET_KEY=CHANGE_ME
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=CHANGE_ME
KEYCLOAK_CLIENT_SECRET_LOCAL=CHANGE_ME
KEYCLOAK_CLIENT_SECRET=CHANGE_ME
BRAVE_API_KEY=CHANGE_ME
SEARXNG_SECRET=CHANGE_ME

# --- OpenWebUI auth ---
ENABLE_LOGIN_FORM=false
ENABLE_OAUTH_SIGNUP=true
DEFAULT_USER_ROLE=user
OAUTH_MERGE_ACCOUNTS_BY_EMAIL=true

# --- Rotating proxy (for SearXNG outbound requests) ---
PROXY_URL=http://owui:CHANGE_ME@163.172.132.16:3128

# --- Web search ---
ENABLE_WEB_SEARCH=true
WEB_SEARCH_ENGINE=searxng
WEB_SEARCH_RESULT_COUNT=5
WEB_SEARCH_CONCURRENT_REQUESTS=3
WEB_SEARCH_TRUST_ENV=false
SEARXNG_QUERY_URL=http://searxng:8080/search
SEARXNG_LANGUAGE=all

# --- K8s ---
NAMESPACE=owui-socle
BASE_DOMAIN=mychat.fake-domain.name
OPENWEBUI_HOST=mychat.fake-domain.name
KEYCLOAK_HOST=mysso.fake-domain.name
SEARXNG_HOST=search.fake-domain.name
LETSENCRYPT_EMAIL=ops@fake-domain.name
TLS_SECRET_NAME=openwebui-tls
KEYCLOAK_TLS_SECRET_NAME=keycloak-tls
SEARXNG_TLS_SECRET_NAME=search-tls

# --- Registry (Scaleway Container Registry) ---
REGISTRY=rg.fr-par.scw.cloud/<your-registry-namespace>
REGISTRY_SERVER=rg.fr-par.scw.cloud
REGISTRY_USERNAME=nologin
REGISTRY_PASSWORD=CHANGE_ME
REGISTRY_EMAIL=ops@fake-domain.name
IMAGE_TAG=0.1.0

# --- Image generation (HuggingFace) ---
# Get your token at https://huggingface.co/settings/tokens
ENABLE_IMAGE_GENERATION=true
HF_TOKEN=hf_CHANGE_ME
HF_IMAGE_MODEL=black-forest-labs/FLUX.1-schnell
HF_TIMEOUT=120
IMAGE_GEN_PORT=9100

# --- Plugin discovery ---
# Comma-separated paths to sibling feature repos (relative to this repo)
PLUGIN_PATHS=../owuipipe-grafragexp,../owuitools-tchapreader,../owuitools-websnap,../owuipipe-anef-legalgraf,../owuitools-dataview,../owuitools-gristmcp

# --- Connexion directe Scaleway (tool calling natif) ---
OPENAI_API_BASE_URLS=http://pipelines:9099/v1;https://api.scaleway.ai/<PROJECT_ID>/v1
OPENAI_API_KEYS=CHANGE_ME;CHANGE_ME
